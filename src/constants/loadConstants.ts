// import * as dotenv from "dotenv";
import mainnetBetaConstants from "./mainnetBetaConstants";

function loadConstants() {
  // check if there is other constants to load
  return mainnetBetaConstants;
}

export default loadConstants;
